---
phase: 04-llm-benchmarking
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/routes/benchmark/-components/ScoringBar.tsx
  - src/routes/benchmark/-components/BenchmarkHistory.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02

must_haves:
  truths:
    - "Scoring bar aparece desabilitada durante streaming e habilita automaticamente quando todas as colunas terminam (allDone)"
    - "Usuário pode selecionar um vencedor ou empate no ToggleGroup — resultado é persistido no SQLite"
    - "Após scoring, bar mostra 'Avaliação registrada' e toast aparece via sonner"
    - "Tab 'Histórico' exibe tabela com prompt truncado a 60 chars, badges de modelos, badge de vencedor correto e data relativa em PT-BR"
    - "Ícone de benchmark aparece na sidebar entre KB e Gmail, rota /benchmark está ativa"
  artifacts:
    - path: "src/routes/benchmark/-components/ScoringBar.tsx"
      provides: "Barra sticky ToggleGroup para scoring com auto-enable"
      contains: "ToggleGroup"
    - path: "src/routes/benchmark/-components/BenchmarkHistory.tsx"
      provides: "Tabela de histórico com badges e data relativa"
      contains: "useBenchmarkHistory"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Entrada benchmark adicionada ao array MODULES"
      contains: "BarChart2"
  key_links:
    - from: "src/routes/benchmark/-components/ScoringBar.tsx"
      to: "src/lib/stores/benchmark.ts"
      via: "useBenchmarkStore setWinner + activeSession selector"
      pattern: "useBenchmarkStore"
    - from: "src/routes/benchmark/-components/BenchmarkHistory.tsx"
      to: "src/lib/queries/benchmark.ts"
      via: "useBenchmarkHistory TanStack Query hook"
      pattern: "useBenchmarkHistory"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/routes/benchmark/index.tsx"
      via: "route='/benchmark' no MODULES array"
      pattern: "benchmark.*implemented.*true"
---

<objective>
Wave 3: implementar ScoringBar (ToggleGroup auto-enable + persistência D-03), BenchmarkHistory (tabela D-04) e ativar o módulo na sidebar (Pitfall 6 do RESEARCH.md).

Purpose: Entrega BENCH-02 (scoring manual persistido e visível no histórico) e fecha o módulo com sidebar ativa.

Output: ScoringBar.tsx (definitivo), BenchmarkHistory.tsx (definitivo), Sidebar.tsx atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-llm-benchmarking/04-CONTEXT.md
@.planning/phases/04-llm-benchmarking/04-RESEARCH.md
@.planning/phases/04-llm-benchmarking/04-UI-SPEC.md
@.planning/phases/04-llm-benchmarking/04-02-SUMMARY.md

@src/components/layout/Sidebar.tsx
@src/components/ui/toggle-group.tsx
@src/components/ui/table.tsx
@src/components/ui/badge.tsx
@src/components/ui/tooltip.tsx
@src/components/ui/sonner.tsx
@src/lib/stores/benchmark.ts
@src/lib/queries/benchmark.ts

<interfaces>
<!-- ToggleGroup API (instalado no Plano 04-02): -->
```typescript
import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group';
// <ToggleGroup type="single" disabled={bool} value={string|undefined} onValueChange={(val) => ...}>
// <ToggleGroupItem value="model-id">Model Name</ToggleGroupItem>
// <ToggleGroupItem value="__tie__">Empate</ToggleGroupItem>
// Sentinel: '__tie__' em ToggleGroup → null em SQLite
```

<!-- useBenchmarkStore selectors relevantes: -->
```typescript
const activeSession = useBenchmarkStore((s) => s.activeSession);
// activeSession.columns: ColumnState[] — cada um tem .status ('idle'|'streaming'|'done'|'error')
// activeSession.winnerId: string | null
// activeSession.scored: boolean
// allDone = activeSession?.columns.every(c => c.status === 'done' || c.status === 'error') ?? false
```

<!-- useBenchmarkHistory return type: -->
```typescript
type BenchmarkSessionWithResults = BenchmarkSession & { results: BenchmarkResult[] };
// BenchmarkSession: { id, prompt, createdAt: Date, scoredAt: Date | null }
// BenchmarkResult: { id, sessionId, model, response, isWinner: boolean, isTie: boolean, createdAt }
```

<!-- sonner toast (já instalado Fase 3): -->
```typescript
import { toast } from 'sonner';
toast('Avaliação registrada.');
```

<!-- date-fns locale ptBR (já instalado): -->
```typescript
import { formatDistanceToNow } from 'date-fns';
import { ptBR } from 'date-fns/locale';
formatDistanceToNow(date, { addSuffix: true, locale: ptBR });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar ScoringBar com ToggleGroup auto-enable e persistência (D-03)</name>
  <files>
    src/routes/benchmark/-components/ScoringBar.tsx
  </files>
  <read_first>
    - src/routes/benchmark/-components/ScoringBar.tsx — ler o stub atual (retorna null)
    - src/components/ui/toggle-group.tsx — API exata do ToggleGroup e ToggleGroupItem
    - src/lib/stores/benchmark.ts — interface completa: activeSession, setWinner, scored, winnerId, columns
    - .planning/phases/04-llm-benchmarking/04-CONTEXT.md §D-03 — especificação exata da scoring bar
    - .planning/phases/04-llm-benchmarking/04-UI-SPEC.md §Scoring bar states e §Interaction Patterns §Scoring
    - .planning/phases/04-llm-benchmarking/04-RESEARCH.md §Pattern 2: Auto-Enabling ToggleGroup
  </read_first>
  <behavior>
    - allDone calculado como selector (não stored state): columns.every(c => c.status === 'done' || c.status === 'error')
    - disabled={!allDone || scored} — bar só ativa quando TODAS as colunas terminaram
    - value usa sentinel '__tie__' para empate; converte para null antes de chamar setWinner
    - Após setWinner: toast('Avaliação registrada.') via sonner
    - Após scoring: ToggleGroup fica disabled novamente (scored=true → disabled)
    - Label acima do ToggleGroup: "Qual resposta foi melhor?" (pré-score) / "Avaliação registrada" (pós-score)
    - Bar: sticky na base, border-t border-border, bg-secondary, h-[52px], py-2 px-4
    - ToggleGroup: 200ms transition-opacity de opacity-50 → opacity-100 quando habilita
  </behavior>
  <action>
Substituir o stub em `src/routes/benchmark/-components/ScoringBar.tsx` com implementação completa:

```typescript
import { toast } from 'sonner';
import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group';
import { useBenchmarkStore } from '@/lib/stores/benchmark';

// Inline model label resolver — same list as BenchmarkPrompt/BenchmarkColumn
const MODEL_LABELS: Record<string, string> = {
  'gpt-4.1': 'GPT-4.1',
  'gpt-4o': 'GPT-4o',
  'claude-opus-4': 'Claude Opus 4',
  'claude-sonnet-4-5': 'Claude Sonnet 4.5',
  'gemini-2.0-flash': 'Gemini 2.0 Flash',
  'gemini-2.5-pro': 'Gemini 2.5 Pro',
};
function getModelLabel(model: string): string {
  return MODEL_LABELS[model] ?? model;
}

export function ScoringBar() {
  const activeSession = useBenchmarkStore((s) => s.activeSession);
  const setWinner = useBenchmarkStore((s) => s.setWinner);

  if (!activeSession) return null;

  const { columns, winnerId, scored } = activeSession;

  // CRITICAL: allDone computed as selector — NOT stored state (avoids race conditions per RESEARCH.md)
  const allDone = columns.every(
    (c) => c.status === 'done' || c.status === 'error'
  );

  const isDisabled = !allDone || scored;

  // ToggleGroup value: use sentinel '__tie__' (null would be "uncontrolled")
  const toggleValue = winnerId === null && scored
    ? '__tie__'
    : (winnerId ?? undefined);

  async function handleValueChange(val: string) {
    if (isDisabled || !val) return;
    // Convert sentinel back to null for SQLite storage
    const modelId = val === '__tie__' ? null : val;
    await setWinner(modelId);
    toast('Avaliação registrada.');
  }

  return (
    // D-03: sticky na base, border-t, bg-secondary, h-[52px]
    <div className="flex h-[52px] shrink-0 items-center justify-between border-t border-border bg-secondary px-4 py-2">
      <span className="text-xs font-medium text-muted-foreground">
        {scored ? 'Avaliação registrada' : 'Qual resposta foi melhor?'}
      </span>

      {/* D-03: ToggleGroup com estados de modelo + Empate
          200ms transition-opacity quando habilita (UI-SPEC §Animation) */}
      <ToggleGroup
        type="single"
        disabled={isDisabled}
        value={toggleValue}
        onValueChange={handleValueChange}
        className="transition-opacity duration-200"
        style={{ opacity: isDisabled ? 0.5 : 1 }}
      >
        {columns.map((col) => (
          <ToggleGroupItem
            key={col.model}
            value={col.model}
            className="h-7 px-3 text-sm font-medium"
          >
            {getModelLabel(col.model)}
          </ToggleGroupItem>
        ))}
        <ToggleGroupItem
          value="__tie__"
          className="h-7 px-3 text-sm font-medium"
        >
          Empate
        </ToggleGroupItem>
      </ToggleGroup>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /root/NexusAI && grep -n "allDone\|columns.every\|__tie__\|setWinner\|toast\|Avaliação registrada\|Qual resposta" src/routes/benchmark/-components/ScoringBar.tsx && pnpm test --run tests/benchmark-store.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "columns.every" src/routes/benchmark/-components/ScoringBar.tsx` retorna 1 linha (allDone como selector)
    - `grep "stored\|useState.*allDone" src/routes/benchmark/-components/ScoringBar.tsx` retorna 0 linhas (allDone NÃO é useState)
    - `grep "__tie__" src/routes/benchmark/-components/ScoringBar.tsx` retorna >= 3 linhas (sentinel em value, onValueChange, ToggleGroupItem)
    - `grep "toast" src/routes/benchmark/-components/ScoringBar.tsx` retorna 1 linha com `'Avaliação registrada.'`
    - `grep "Qual resposta foi melhor" src/routes/benchmark/-components/ScoringBar.tsx` retorna 1 linha (copywriting PT-BR correto)
    - `grep "transition-opacity duration-200" src/routes/benchmark/-components/ScoringBar.tsx` retorna 1 linha (animation spec)
    - `pnpm exec biome check src/routes/benchmark/-components/ScoringBar.tsx` sem erros
  </acceptance_criteria>
  <done>ScoringBar implementada: auto-enable via allDone selector, __tie__ sentinel, persistência via setWinner, toast de confirmação.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar BenchmarkHistory (tabela D-04) e ativar módulo na sidebar</name>
  <files>
    src/routes/benchmark/-components/BenchmarkHistory.tsx
    src/components/layout/Sidebar.tsx
  </files>
  <read_first>
    - src/routes/benchmark/-components/BenchmarkHistory.tsx — ler o stub atual
    - src/components/layout/Sidebar.tsx — ler COMPLETO para inserir benchmark no array MODULES na posição correta (após "kb", antes de "gmail")
    - src/components/ui/table.tsx — API do shadcn Table (Table, TableHeader, TableRow, TableHead, TableBody, TableCell)
    - src/components/ui/badge.tsx — Badge com variant="default", "secondary", "outline"
    - src/lib/queries/benchmark.ts — interface useBenchmarkHistory e tipo BenchmarkSessionWithResults
    - .planning/phases/04-llm-benchmarking/04-CONTEXT.md §D-04 — especificação exata da tabela de histórico
    - .planning/phases/04-llm-benchmarking/04-UI-SPEC.md §History tab e §Copywriting Contract
    - .planning/phases/04-llm-benchmarking/04-RESEARCH.md §Pitfall 6 — sidebar entry obrigatória
  </read_first>
  <behavior>
    - Tabela: 4 colunas — "Prompt", "Modelos", "Vencedor", "Data"
    - Prompt: truncado em 60 chars + "…", tooltip com texto completo
    - Modelos: Badge por modelo (text-xs font-medium)
    - Vencedor: Badge variant="default" para model vencedor; variant="secondary" para "Empate"; variant="outline" + text-muted-foreground para "Não avaliado"
    - Data: formatDistanceToNow(date, { addSuffix: true, locale: ptBR }), tooltip com timestamp ISO
    - Empty state: "Nenhum benchmark registrado" / "Inicie sua primeira sessão na aba "Nova sessão"."
    - Sidebar: adicionar benchmark com icon=BarChart2, route="/benchmark", implemented=true, label="Benchmark" — POSIÇÃO: após "kb" (índice 2, antes de "gmail")
  </behavior>
  <action>
**1. Substituir o stub em `src/routes/benchmark/-components/BenchmarkHistory.tsx`** com implementação completa:

```typescript
import { formatDistanceToNow } from 'date-fns';
import { ptBR } from 'date-fns/locale';
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table';
import { Badge } from '@/components/ui/badge';
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip';
import { ScrollArea } from '@/components/ui/scroll-area';
import { useBenchmarkHistory, type BenchmarkSessionWithResults } from '@/lib/queries/benchmark';
import type { BenchmarkResult } from '@/lib/db/schema';

// Inline label resolver — consistent with ScoringBar and BenchmarkColumn
const MODEL_LABELS: Record<string, string> = {
  'gpt-4.1': 'GPT-4.1',
  'gpt-4o': 'GPT-4o',
  'claude-opus-4': 'Claude Opus 4',
  'claude-sonnet-4-5': 'Claude Sonnet 4.5',
  'gemini-2.0-flash': 'Gemini 2.0 Flash',
  'gemini-2.5-pro': 'Gemini 2.5 Pro',
};
function getModelLabel(model: string): string {
  return MODEL_LABELS[model] ?? model;
}

function truncatePrompt(text: string, maxLen = 60): string {
  return text.length > maxLen ? text.slice(0, maxLen) + '…' : text;
}

// D-04: winner badge logic (per UI-SPEC §Color §Winner badge na história)
function WinnerBadge({ results }: { results: BenchmarkResult[] }) {
  const winner = results.find((r) => r.isWinner);
  const tie = results.find((r) => r.isTie);
  if (winner) {
    return (
      <Badge variant="default" className="text-xs font-medium">
        {getModelLabel(winner.model)}
      </Badge>
    );
  }
  if (tie) {
    return (
      <Badge variant="secondary" className="text-xs font-medium">
        Empate
      </Badge>
    );
  }
  return (
    <Badge variant="outline" className="text-xs font-medium text-muted-foreground">
      Não avaliado
    </Badge>
  );
}

function HistoryRow({ session }: { session: BenchmarkSessionWithResults }) {
  const truncated = truncatePrompt(session.prompt);
  const isLong = session.prompt.length > 60;
  const createdAt = session.createdAt instanceof Date
    ? session.createdAt
    : new Date(session.createdAt);

  const relativeDate = formatDistanceToNow(createdAt, {
    addSuffix: true,
    locale: ptBR,
  });
  const absoluteDate = createdAt.toLocaleString('pt-BR');

  return (
    <TableRow className="h-10">
      {/* Prompt — 60 chars truncated with tooltip (D-04) */}
      <TableCell className="px-4 text-sm font-normal">
        {isLong ? (
          <Tooltip>
            <TooltipTrigger asChild>
              <span className="cursor-default">{truncated}</span>
            </TooltipTrigger>
            <TooltipContent side="top" className="max-w-sm">
              {session.prompt}
            </TooltipContent>
          </Tooltip>
        ) : (
          <span>{truncated}</span>
        )}
      </TableCell>

      {/* Models — one badge per result row */}
      <TableCell className="px-4">
        <div className="flex flex-wrap gap-1">
          {session.results.map((r) => (
            <Badge
              key={r.id}
              variant="secondary"
              className="text-xs font-medium"
            >
              {getModelLabel(r.model)}
            </Badge>
          ))}
        </div>
      </TableCell>

      {/* Winner badge */}
      <TableCell className="px-4">
        <WinnerBadge results={session.results} />
      </TableCell>

      {/* Date — relative with absolute tooltip */}
      <TableCell className="px-4">
        <Tooltip>
          <TooltipTrigger asChild>
            <span className="text-xs text-muted-foreground cursor-default">
              {relativeDate}
            </span>
          </TooltipTrigger>
          <TooltipContent side="top">{absoluteDate}</TooltipContent>
        </Tooltip>
      </TableCell>
    </TableRow>
  );
}

export function BenchmarkHistory() {
  const { data: sessions, isLoading } = useBenchmarkHistory();

  if (isLoading) {
    return (
      <div className="flex flex-1 items-center justify-center p-8">
        <p className="text-sm text-muted-foreground">Carregando histórico...</p>
      </div>
    );
  }

  if (!sessions || sessions.length === 0) {
    return (
      <div className="flex flex-1 flex-col items-center justify-center gap-2 p-8 text-center">
        <p className="text-base font-medium text-foreground">
          Nenhum benchmark registrado
        </p>
        <p className="text-sm text-muted-foreground">
          Inicie sua primeira sessão na aba &ldquo;Nova sessão&rdquo;.
        </p>
      </div>
    );
  }

  return (
    <ScrollArea className="h-full">
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead className="px-4">Prompt</TableHead>
            <TableHead className="px-4">Modelos</TableHead>
            <TableHead className="px-4">Vencedor</TableHead>
            <TableHead className="px-4">Data</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {sessions.map((session) => (
            <HistoryRow key={session.id} session={session} />
          ))}
        </TableBody>
      </Table>
    </ScrollArea>
  );
}
```

**2. Atualizar `src/components/layout/Sidebar.tsx`** — adicionar benchmark ao array MODULES:

Ler o arquivo completo. Localizar o array MODULES. Modificar dois lugares:

**a) Adicionar import de BarChart2:**
Na linha de imports do lucide-react, adicionar `BarChart2` ao destructuring existente:
```typescript
import {
  MessageSquare,  // Chat
  BookOpen,       // KB (Knowledge Base)
  BarChart2,      // Benchmark  ← ADICIONAR
  Mail,           // Gmail
  CalendarDays,   // Calendar
  Plug,           // MCP
  Bot,            // Agents
  Settings,       // Settings gear
} from "lucide-react";
```

**b) Inserir entry de benchmark APÓS "kb" e ANTES de "gmail" no array MODULES:**
```typescript
export const MODULES: SidebarModule[] = [
  { id: "chat",      icon: MessageSquare, label: "Chat",                 route: "/chat",      implemented: true },
  { id: "kb",        icon: BookOpen,      label: "Base de Conhecimento", route: "/kb",        implemented: true },
  { id: "benchmark", icon: BarChart2,     label: "Benchmark",            route: "/benchmark", implemented: true },  // ← ADICIONAR
  { id: "gmail",     icon: Mail,          label: "Gmail",                route: "/gmail",     implemented: false },
  { id: "calendar",  icon: CalendarDays,  label: "Calendário",           route: "/calendar",  implemented: false },
  { id: "mcp",       icon: Plug,          label: "MCPs",                 route: "/mcp",       implemented: false },
  { id: "agents",    icon: Bot,           label: "Agentes",              route: "/agents",    implemented: false },
];
```

**IMPORTANTE:** Não alterar nenhum outro código do Sidebar.tsx — apenas os dois pontos acima (import e array entry).
  </action>
  <verify>
    <automated>cd /root/NexusAI && grep -n "benchmark.*BarChart2\|BarChart2.*benchmark" src/components/layout/Sidebar.tsx && grep -n "implemented: true.*benchmark\|benchmark.*implemented: true" src/components/layout/Sidebar.tsx && grep -n "useBenchmarkHistory\|WinnerBadge\|truncatePrompt" src/routes/benchmark/-components/BenchmarkHistory.tsx && pnpm test --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BarChart2" src/components/layout/Sidebar.tsx` retorna 2 linhas (import + uso no MODULES array)
    - `grep "benchmark.*implemented.*true\|implemented.*true.*benchmark" src/components/layout/Sidebar.tsx` retorna 1 linha
    - A posição de benchmark no MODULES array está APÓS "kb" e ANTES de "gmail" (verificar com: `grep -n "kb\|benchmark\|gmail" src/components/layout/Sidebar.tsx`)
    - `grep "useBenchmarkHistory" src/routes/benchmark/-components/BenchmarkHistory.tsx` retorna 1 linha
    - `grep "WinnerBadge\|variant=\"default\"\|variant=\"secondary\"\|variant=\"outline\"" src/routes/benchmark/-components/BenchmarkHistory.tsx` retorna >= 3 linhas
    - `grep "formatDistanceToNow\|ptBR\|locale" src/routes/benchmark/-components/BenchmarkHistory.tsx` retorna >= 2 linhas
    - `grep "Não avaliado\|Empate\|Nenhum benchmark registrado" src/routes/benchmark/-components/BenchmarkHistory.tsx` retorna 3 linhas (copywriting PT-BR)
    - `grep "truncatePrompt\|60\|…" src/routes/benchmark/-components/BenchmarkHistory.tsx` retorna >= 2 linhas
    - `pnpm test --run` sem novos failures
    - `pnpm exec biome check src/routes/benchmark/-components/BenchmarkHistory.tsx src/components/layout/Sidebar.tsx` sem erros
  </acceptance_criteria>
  <done>BenchmarkHistory com tabela completa (D-04) + Sidebar com benchmark ativo entre KB e Gmail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScoringBar → setWinner → saveBenchmarkScore → SQLite | modelId vem do ToggleGroupItem value — limitado aos models do activeSession |
| BenchmarkHistory ← SQLite | Leitura de dados locais via Drizzle; nenhum dado de terceiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | ScoringBar onValueChange (winnerId) | mitigate | ToggleGroupItem.value é limitado aos model IDs de activeSession.columns — UI não aceita valor arbitrário; setWinner no store valida que modelId pertence à session antes de persistir (T-04-01-01 do Plano 01) |
| T-04-03-02 | Information Disclosure | BenchmarkHistory tabela | accept | Dados de sessões anteriores são do próprio usuário; SQLite local; sem risco de divulgação a terceiros |
| T-04-03-03 | Tampering | Sidebar route ativação | accept | Rota /benchmark é código estático compilado; nenhum input de usuário determina quais rotas ficam ativas |
</threat_model>

<verification>
```bash
cd /root/NexusAI
# Sidebar entry correta
grep -n "benchmark\|BarChart2" src/components/layout/Sidebar.tsx
# Posição correta (deve mostrar kb antes de benchmark, benchmark antes de gmail)
grep -n "\"kb\"\|\"benchmark\"\|\"gmail\"" src/components/layout/Sidebar.tsx

# ScoringBar constraints
grep -n "allDone\|columns.every\|__tie__" src/routes/benchmark/-components/ScoringBar.tsx

# History table
grep -n "formatDistanceToNow\|ptBR\|WinnerBadge\|truncatePrompt" src/routes/benchmark/-components/BenchmarkHistory.tsx

# Suite completa
pnpm test --run 2>&1 | tail -5
```
</verification>

<success_criteria>
- ScoringBar implementada com allDone selector (não estado), ToggleGroup auto-enable, __tie__ sentinel, toast 'Avaliação registrada.'
- BenchmarkHistory com tabela completa: prompt truncado/tooltip, badges de modelos, WinnerBadge (3 variantes), data relativa ptBR/tooltip absoluto
- Empty state correto em ambos os casos (sem sessões)
- Sidebar com entrada benchmark (BarChart2, /benchmark, implemented: true) entre KB e Gmail
- `pnpm test --run` verde (todos os testes de benchmark passam)
- `pnpm exec biome check` sem erros em todos os arquivos modificados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-llm-benchmarking/04-03-SUMMARY.md`
</output>
