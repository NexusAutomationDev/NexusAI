---
phase: 04-llm-benchmarking
plan: "01"
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - src/lib/stores/benchmark.ts
  - src/lib/queries/benchmark.ts
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02

must_haves:
  truths:
    - "benchmarkStore rastreia estado por coluna (model, content, status, error, syntheticId)"
    - "appendToken acumula texto na coluna correta sem afetar outras colunas"
    - "allDone é calculado como selector, não como estado armazenado — sem race condition"
    - "useBenchmarkHistory retorna sessões ordenadas por created_at DESC com results aninhados"
  artifacts:
    - path: "src/lib/stores/benchmark.ts"
      provides: "Zustand store completo para sessão de benchmark"
      exports: ["useBenchmarkStore", "ColumnState", "BenchmarkSession", "BenchmarkStore"]
    - path: "src/lib/queries/benchmark.ts"
      provides: "TanStack Query hooks para histórico e persistência"
      exports: ["useBenchmarkHistory", "saveBenchmarkScore"]
  key_links:
    - from: "src/lib/stores/benchmark.ts"
      to: "src/lib/db/schema.ts"
      via: "import { benchmarkSessions, benchmarkResults } from '@/lib/db/schema'"
      pattern: "benchmarkSessions|benchmarkResults"
    - from: "src/lib/queries/benchmark.ts"
      to: "src/lib/db/proxy.ts"
      via: "import { db } from '@/lib/db/proxy'"
      pattern: "from.*db/proxy"
---

<objective>
Wave 1: implementar o Zustand benchmarkStore e os TanStack Query hooks de histórico/persistência.

Purpose: O store é a fonte de verdade para estado de sessão ativa — outros planos dependem dele para renderizar colunas e controlar o scoring.

Output: src/lib/stores/benchmark.ts + src/lib/queries/benchmark.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-llm-benchmarking/04-CONTEXT.md
@.planning/phases/04-llm-benchmarking/04-RESEARCH.md
@.planning/phases/04-llm-benchmarking/04-00-SUMMARY.md

@src/lib/stores/chat.ts
@src/lib/stores/settings.ts
@src/lib/db/schema.ts
@src/lib/db/proxy.ts

<interfaces>
<!-- Tipos Drizzle criados no Plano 04-00 -->
<!-- From src/lib/db/schema.ts: -->
```typescript
export const benchmarkSessions = sqliteTable('benchmark_sessions', {
  id: text('id').primaryKey(),
  prompt: text('prompt').notNull(),
  createdAt: integer('created_at', { mode: 'timestamp' }).notNull(),
  scoredAt: integer('scored_at', { mode: 'timestamp' }),
});
export const benchmarkResults = sqliteTable('benchmark_results', {
  id: text('id').primaryKey(),
  sessionId: text('session_id').notNull().references(() => benchmarkSessions.id, { onDelete: 'cascade' }),
  model: text('model').notNull(),
  response: text('response').notNull(),
  isWinner: integer('is_winner', { mode: 'boolean' }).notNull().default(false),
  isTie: integer('is_tie', { mode: 'boolean' }).notNull().default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).notNull(),
});
export type BenchmarkSession = typeof benchmarkSessions.$inferSelect;
export type BenchmarkResult = typeof benchmarkResults.$inferSelect;
```

<!-- From src/lib/stores/chat.ts — padrão a seguir: -->
```typescript
import { create } from 'zustand';
import { invoke, Channel } from '@tauri-apps/api/core';
// Usa create<Store>((set, get) => ({ ... }))
// Usa Channel<StreamEvent> para streaming
// Chama invoke('stream_chat', { input, onEvent: channel })
// Chama invoke('stop_streaming', { conversationId })
```

<!-- From src/lib/db/proxy.ts — padrão de query: -->
```typescript
import { db } from '@/lib/db/proxy';
// Usa Drizzle: db.select().from(table).where(...).orderBy(...)
// Usa Drizzle: db.insert(table).values({ ... })
// Usa Drizzle: db.update(table).set({ ... }).where(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar benchmarkStore (Zustand) com gestão de sessão e streaming por coluna</name>
  <files>
    src/lib/stores/benchmark.ts
  </files>
  <read_first>
    - src/lib/stores/chat.ts — estrutura exata de create<Store>, Channel pattern, invoke calls
    - src/lib/stores/settings.ts — como useSettingsStore.getState() é usado para defaults
    - .planning/phases/04-llm-benchmarking/04-RESEARCH.md §Zustand benchmarkStore Shape — interface completa definida pelo pesquisador
  </read_first>
  <behavior>
    - startSession(prompt, models): cria BenchmarkSession com ColumnState[] idle, retorna sessionId
    - appendToken(colIdx, text): acumula em columns[colIdx].content (imutável — spread/novo array)
    - setColumnDone(colIdx): columns[colIdx].status = 'done'
    - setColumnError(colIdx, msg): columns[colIdx].status = 'error', .error = msg
    - setColumnStreaming(colIdx): columns[colIdx].status = 'streaming'
    - stopColumn(colIdx): invoke('stop_streaming', { conversationId: syntheticId })
    - stopAll(): chama stopColumn para cada coluna com syntheticId definido
    - setWinner(modelId | '__tie__'): set scored=true, winnerId, chama saveBenchmarkScore via import dinâmico
    - resetSession(): activeSession = null
    - allDone NÃO é estado armazenado — é um selector calculado externamente
  </behavior>
  <action>
Criar `src/lib/stores/benchmark.ts` com o seguinte conteúdo completo:

```typescript
/**
 * Benchmark Zustand store — manages active benchmark session state.
 * Follows the same pattern as src/lib/stores/chat.ts.
 *
 * IMPORTANT: allDone is NOT stored state — compute as selector to avoid race conditions:
 * useStore(useBenchmarkStore, s => s.activeSession?.columns.every(
 *   c => c.status === 'done' || c.status === 'error'
 * ) ?? false)
 *
 * IMPORTANT: Each benchmark column uses N separate stream_chat invocations with
 * synthetic conversationIds (sessionId + '-col-' + colIdx) for per-column stop support.
 * See RESEARCH.md §Architecture Patterns for rationale.
 */

import { create } from 'zustand';
import { invoke } from '@tauri-apps/api/core';

export type ColumnStatus = 'idle' | 'streaming' | 'done' | 'error';

export interface ColumnState {
  model: string;
  content: string;         // accumulated token text
  status: ColumnStatus;
  error?: string;
  syntheticId?: string;    // sessionId-col-N, for stop_streaming lookup
}

export interface ActiveBenchmarkSession {
  sessionId: string;
  prompt: string;
  columns: ColumnState[];  // length 2–4
  winnerId: string | null; // model ID, '__tie__', or null (not scored yet)
  scored: boolean;
}

interface BenchmarkStore {
  activeSession: ActiveBenchmarkSession | null;

  startSession: (prompt: string, models: string[]) => string;
  appendToken: (colIdx: number, text: string) => void;
  setColumnDone: (colIdx: number) => void;
  setColumnError: (colIdx: number, msg: string) => void;
  setColumnStreaming: (colIdx: number) => void;
  setColumnSyntheticId: (colIdx: number, id: string) => void;
  stopColumn: (colIdx: number) => Promise<void>;
  stopAll: () => Promise<void>;
  setWinner: (modelId: string | null) => Promise<void>;
  resetSession: () => void;
}

export const useBenchmarkStore = create<BenchmarkStore>((set, get) => ({
  activeSession: null,

  startSession: (prompt, models) => {
    const sessionId = crypto.randomUUID();
    const columns: ColumnState[] = models.map((model) => ({
      model,
      content: '',
      status: 'idle',
    }));
    set({
      activeSession: {
        sessionId,
        prompt,
        columns,
        winnerId: null,
        scored: false,
      },
    });
    return sessionId;
  },

  appendToken: (colIdx, text) =>
    set((s) => {
      if (!s.activeSession) return s;
      const columns = s.activeSession.columns.map((col, i) =>
        i === colIdx ? { ...col, content: col.content + text } : col
      );
      return { activeSession: { ...s.activeSession, columns } };
    }),

  setColumnDone: (colIdx) =>
    set((s) => {
      if (!s.activeSession) return s;
      const columns = s.activeSession.columns.map((col, i) =>
        i === colIdx ? { ...col, status: 'done' as ColumnStatus } : col
      );
      return { activeSession: { ...s.activeSession, columns } };
    }),

  setColumnError: (colIdx, msg) =>
    set((s) => {
      if (!s.activeSession) return s;
      const columns = s.activeSession.columns.map((col, i) =>
        i === colIdx ? { ...col, status: 'error' as ColumnStatus, error: msg } : col
      );
      return { activeSession: { ...s.activeSession, columns } };
    }),

  setColumnStreaming: (colIdx) =>
    set((s) => {
      if (!s.activeSession) return s;
      const columns = s.activeSession.columns.map((col, i) =>
        i === colIdx ? { ...col, status: 'streaming' as ColumnStatus } : col
      );
      return { activeSession: { ...s.activeSession, columns } };
    }),

  setColumnSyntheticId: (colIdx, id) =>
    set((s) => {
      if (!s.activeSession) return s;
      const columns = s.activeSession.columns.map((col, i) =>
        i === colIdx ? { ...col, syntheticId: id } : col
      );
      return { activeSession: { ...s.activeSession, columns } };
    }),

  stopColumn: async (colIdx) => {
    const { activeSession } = get();
    if (!activeSession) return;
    const col = activeSession.columns[colIdx];
    if (!col?.syntheticId) return;
    try {
      await invoke('stop_streaming', { conversationId: col.syntheticId });
    } catch {
      // Ignore — stream may have already finished
    }
  },

  stopAll: async () => {
    const { activeSession, stopColumn } = get();
    if (!activeSession) return;
    await Promise.allSettled(
      activeSession.columns.map((_, idx) => stopColumn(idx))
    );
  },

  setWinner: async (modelId) => {
    const { activeSession } = get();
    if (!activeSession || activeSession.scored) return;

    // Update UI state synchronously
    set((s) => ({
      activeSession: s.activeSession
        ? { ...s.activeSession, winnerId: modelId, scored: true }
        : null,
    }));

    // Persist to SQLite via queries/benchmark.ts
    try {
      const { saveBenchmarkScore } = await import('@/lib/queries/benchmark');
      await saveBenchmarkScore({
        session: activeSession,
        winnerId: modelId,
      });
    } catch (e) {
      console.error('[benchmarkStore] Failed to persist score:', e);
    }
  },

  resetSession: () => set({ activeSession: null }),
}));
```
  </action>
  <verify>
    <automated>cd /root/NexusAI && pnpm test --run tests/benchmark-store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/benchmark-store.test.ts` passa em todos os testes (exceto o que importa queries/benchmark — esse continua RED até Task 2)
    - `grep -n "export const useBenchmarkStore" src/lib/stores/benchmark.ts` retorna uma linha
    - `grep -n "export.*ColumnState\|export.*ActiveBenchmarkSession\|export.*ColumnStatus" src/lib/stores/benchmark.ts` retorna 3 linhas
    - `grep -n "syntheticId" src/lib/stores/benchmark.ts | wc -l` retorna >= 3 (campo, setter, uso em stop)
    - `grep -n "allDone" src/lib/stores/benchmark.ts` retorna 0 (allDone NÃO é stored state)
    - `pnpm exec biome check src/lib/stores/benchmark.ts` retorna sem erros
  </acceptance_criteria>
  <done>benchmarkStore criado e exportado; testes RED de stores passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar TanStack Query hooks para histórico e persistência de score</name>
  <files>
    src/lib/queries/benchmark.ts
  </files>
  <read_first>
    - src/lib/db/schema.ts — tipos BenchmarkSession, BenchmarkResult, tables benchmarkSessions/benchmarkResults
    - src/lib/db/proxy.ts — import { db } e padrão de query Drizzle
    - src/lib/stores/benchmark.ts (recém criado) — interface ActiveBenchmarkSession (usada por saveBenchmarkScore)
    - .planning/phases/04-llm-benchmarking/04-RESEARCH.md §Pattern 3: History Table with TanStack Query
  </read_first>
  <behavior>
    - useBenchmarkHistory: queryKey=['benchmark-history'], staleTime: 30_000, ordena por created_at DESC, limite 100, aninha results por sessionId
    - saveBenchmarkScore: insere benchmark_sessions + N benchmark_results rows, invalida cache 'benchmark-history'
    - Tipo de retorno de useBenchmarkHistory: Array<BenchmarkSession & { results: BenchmarkResult[] }>
    - saveBenchmarkScore recebe { session: ActiveBenchmarkSession, winnerId: string | null }
    - '__tie__' sentinel é convertido para null ao determinar is_tie/is_winner
  </behavior>
  <action>
Criar `src/lib/queries/benchmark.ts` com o seguinte conteúdo:

```typescript
/**
 * TanStack Query hooks for benchmark history and score persistence.
 * Follows the same patterns as src/lib/queries/chat.ts.
 *
 * useBenchmarkHistory — fetch up to 100 past sessions (D-04: flat list, no pagination in v1)
 * saveBenchmarkScore — insert session + results rows, invalidate cache
 */

import { useQuery, useQueryClient } from '@tanstack/react-query';
import { desc, inArray } from 'drizzle-orm';
import { db } from '@/lib/db/proxy';
import {
  benchmarkSessions,
  benchmarkResults,
  type BenchmarkSession,
  type BenchmarkResult,
} from '@/lib/db/schema';
import type { ActiveBenchmarkSession } from '@/lib/stores/benchmark';

export type BenchmarkSessionWithResults = BenchmarkSession & {
  results: BenchmarkResult[];
};

export function useBenchmarkHistory() {
  return useQuery<BenchmarkSessionWithResults[]>({
    queryKey: ['benchmark-history'],
    staleTime: 30_000,
    queryFn: async (): Promise<BenchmarkSessionWithResults[]> => {
      const sessions = await db
        .select()
        .from(benchmarkSessions)
        .orderBy(desc(benchmarkSessions.createdAt))
        .limit(100);

      if (sessions.length === 0) return [];

      const results = await db
        .select()
        .from(benchmarkResults)
        .where(inArray(benchmarkResults.sessionId, sessions.map((s) => s.id)));

      return sessions.map((s) => ({
        ...s,
        results: results.filter((r) => r.sessionId === s.id),
      }));
    },
  });
}

interface SaveBenchmarkScoreParams {
  session: ActiveBenchmarkSession;
  winnerId: string | null; // null = tie; string = model ID (not '__tie__' sentinel)
}

/**
 * Persist a completed benchmark session and its column results to SQLite.
 * Called by benchmarkStore.setWinner() after updating UI state.
 *
 * Uses winnerId=null to mean tie (is_tie=true on all result rows).
 * Uses winnerId=modelId to mean that specific model won (is_winner=true on its row).
 */
export async function saveBenchmarkScore({
  session,
  winnerId,
}: SaveBenchmarkScoreParams): Promise<void> {
  const now = new Date();
  const isTie = winnerId === null;

  // Insert session row
  await db.insert(benchmarkSessions).values({
    id: session.sessionId,
    prompt: session.prompt,
    createdAt: now,
    scoredAt: now,
  });

  // Insert one result row per column
  const rows = session.columns.map((col) => ({
    id: crypto.randomUUID(),
    sessionId: session.sessionId,
    model: col.model,
    response: col.content,
    isWinner: !isTie && winnerId === col.model,
    isTie,
    createdAt: now,
  }));

  await db.insert(benchmarkResults).values(rows);
}

/**
 * Hook-based version for use inside React components that need to invalidate cache.
 * For non-hook contexts (store), use saveBenchmarkScore directly.
 */
export function useSaveBenchmarkScore() {
  const queryClient = useQueryClient();

  return async (params: SaveBenchmarkScoreParams): Promise<void> => {
    await saveBenchmarkScore(params);
    await queryClient.invalidateQueries({ queryKey: ['benchmark-history'] });
  };
}
```
  </action>
  <verify>
    <automated>cd /root/NexusAI && pnpm test --run tests/benchmark-history.test.ts tests/benchmark-store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/benchmark-history.test.ts` passa em TODOS os testes (o import de queries/benchmark resolve agora)
    - `tests/benchmark-store.test.ts` passa em todos os testes (o setWinner que usa import dinâmico de queries/benchmark deve passar)
    - `grep -n "export function useBenchmarkHistory\|export async function saveBenchmarkScore\|export function useSaveBenchmarkScore" src/lib/queries/benchmark.ts` retorna 3 linhas
    - `grep -n "inArray\|desc\|benchmarkSessions\|benchmarkResults" src/lib/queries/benchmark.ts` retorna >= 4 linhas
    - `pnpm exec biome check src/lib/queries/benchmark.ts` retorna sem erros
    - Suite completa `pnpm test --run` não tem novos failures além dos pré-existentes
  </acceptance_criteria>
  <done>Hooks de TanStack Query para benchmark criados; todos os testes de benchmark passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmarkStore → stream_chat (Rust) | syntheticId é gerado internamente via sessionId + colIdx — não deriva de input do usuário sem sanitização |
| saveBenchmarkScore → SQLite | Dados escritos vêm do estado interno do store (response acumulado, modelo selecionado pelo usuário) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | benchmarkStore.setWinner | mitigate | winnerId validado: só pode ser null ou um dos model IDs no activeSession.columns — nunca string arbitrária. Implementar validação: `if (winnerId !== null && !session.columns.find(c => c.model === winnerId)) return;` |
| T-04-01-02 | Tampering | saveBenchmarkScore SQL insert | accept | Drizzle parameterized queries previnem SQL injection; dados vêm do estado do store (não de input direto do usuário na função) |
| T-04-01-03 | Information Disclosure | benchmark_results.response | accept | Respostas do LLM ficam em SQLite local; app é local-first sem sync; risco é zero neste layer |
</threat_model>

<verification>
```bash
cd /root/NexusAI
# Confirmar exports do store
grep -n "export const useBenchmarkStore\|export.*ColumnState\|export.*ActiveBenchmarkSession" src/lib/stores/benchmark.ts
# Confirmar exports das queries
grep -n "export function useBenchmarkHistory\|export async function saveBenchmarkScore" src/lib/queries/benchmark.ts
# Rodar testes benchmark
pnpm test --run tests/benchmark-store.test.ts tests/benchmark-streaming.test.ts tests/benchmark-history.test.ts
# Suite completa
pnpm test --run 2>&1 | tail -5
```
</verification>

<success_criteria>
- `src/lib/stores/benchmark.ts` existe com useBenchmarkStore, ColumnState, ActiveBenchmarkSession exportados
- `src/lib/queries/benchmark.ts` existe com useBenchmarkHistory, saveBenchmarkScore, useSaveBenchmarkScore exportados
- `allDone` não aparece como estado armazenado no store (é selector externo)
- Todos os testes de benchmark passam (benchmark-store, benchmark-streaming, benchmark-history)
- `pnpm exec biome check` sem erros nos dois novos arquivos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-llm-benchmarking/04-01-SUMMARY.md`
</output>
