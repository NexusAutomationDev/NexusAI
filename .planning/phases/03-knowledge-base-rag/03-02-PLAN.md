---
phase: 03-knowledge-base-rag
plan: 02
type: execute
wave: 2
depends_on: ["03-00"]
files_modified:
  - src-tauri/crates/nexusai-kb/src/embed.rs
  - src-tauri/crates/nexusai-kb/src/vector.rs
  - src-tauri/crates/nexusai-kb/src/search.rs
autonomous: true
requirements: [KB-02, KB-07]
must_haves:
  truths:
    - "Text embeds locally via fastembed (MultilingualE5Small, 384-dim) with no external API at query time"
    - "Vectors insert into sqlite-vec vec0 table and KNN returns the nearest chunk rowids"
    - "BM25 (FTS5) and vector ranks fuse via RRF (k=60) into a single deterministic ordering"
  artifacts:
    - path: "src-tauri/crates/nexusai-kb/src/embed.rs"
      provides: "fastembed singleton, passage/query embedding with E5 prefixes"
      contains: "MultilingualE5Small"
    - path: "src-tauri/crates/nexusai-kb/src/vector.rs"
      provides: "THE single sqlite-vec module (D-16): load, vec0 table, insert, KNN"
      contains: "sqlite3_auto_extension"
    - path: "src-tauri/crates/nexusai-kb/src/search.rs"
      provides: "FTS5 bm25 query + RRF fusion"
      contains: "reciprocal_rank_fusion"
  key_links:
    - from: "src-tauri/crates/nexusai-kb/src/search.rs"
      to: "src-tauri/crates/nexusai-kb/src/vector.rs"
      via: "search calls vector::knn and fuses with bm25 ranks"
      pattern: "vector::knn|knn\\("
---

<objective>
Implement the retrieval engine of nexusai-kb: local ONNX embeddings (fastembed E5, PT-BR-capable), the single isolated sqlite-vec module (D-16), and the FTS5 BM25 + RRF hybrid search. Turns GREEN the `cargo test -p nexusai-kb vector`, `rrf`, and (`--ignored`) `offline` RED tests from Plan 03-00.

Purpose: This is the heart of KB-02 (hybrid retrieval) and KB-07 (local offline embeddings). All sqlite-vec API surface is confined to vector.rs per D-16 / STATE.md (alpha library isolation).
Output: embed.rs, vector.rs, search.rs implemented; vector + rrf tests green, offline test ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-knowledge-base-rag/03-RESEARCH.md
@.planning/phases/03-knowledge-base-rag/03-CONTEXT.md

<interfaces>
<!-- Contracts from Plan 03-00's RED tests + verbatim reference code from RESEARCH. -->

vector.rs (test_knn_roundtrip expects):
```rust
pub fn register_sqlite_vec();
pub fn init_vec_table(db: &rusqlite::Connection) -> rusqlite::Result<()>;
pub fn insert_vector(db: &rusqlite::Connection, chunk_rowid: i64, emb: &[f32]) -> rusqlite::Result<()>;
pub fn knn(db: &rusqlite::Connection, query: &[f32], k: usize) -> rusqlite::Result<Vec<(i64, f64)>>;
```

search.rs (test_rrf_deterministic expects):
```rust
pub fn reciprocal_rank_fusion(vec_hits: &[i64], bm25_hits: &[i64], k: f64, top_n: usize) -> Vec<i64>;
```

embed.rs (offline test expects):
```rust
pub fn load_model(cache_dir: std::path::PathBuf) -> anyhow::Result<fastembed::TextEmbedding>;
pub fn embed_passages(model: &fastembed::TextEmbedding, texts: &[String]) -> anyhow::Result<Vec<Vec<f32>>>;
pub fn embed_query(model: &fastembed::TextEmbedding, q: &str) -> anyhow::Result<Vec<f32>>;
```

Reference code is in 03-RESEARCH.md Patterns 1, 2, 3 (verbatim from sqlite-vec/fastembed official docs — use them directly).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sqlite-vec module (vector.rs) — D-16 isolation</name>
  <read_first>
    - src-tauri/crates/nexusai-kb/src/vector.rs (RED test_knn_roundtrip + unimplemented stubs from Plan 03-00)
    - .planning/phases/03-knowledge-base-rag/03-RESEARCH.md (§Architecture Pattern 1 — verbatim sqlite-vec load/insert/KNN code; Pitfall 1 rusqlite 0.34 trap; Pitfall 3 fixed dimension)
    - src-tauri/Cargo.toml (confirms rusqlite 0.31 bundled — do NOT bump)
  </read_first>
  <behavior>
    - test_knn_roundtrip (GREEN): in-memory Connection, register+init, insert 3 known 384-dim vectors, KNN(query≈vector#2, k=1) returns vector #2's rowid.
  </behavior>
  <action>
    Implement src-tauri/crates/nexusai-kb/src/vector.rs EXACTLY following 03-RESEARCH.md Pattern 1 (it is verbatim-verified official code). This file is the ONLY place that imports `sqlite_vec` (D-16):

    - `pub fn register_sqlite_vec()`: `unsafe { sqlite3_auto_extension(Some(std::mem::transmute(sqlite3_vec_init as *const ()))); }` using `rusqlite::ffi::sqlite3_auto_extension` and `sqlite_vec::sqlite3_vec_init`. Add a comment: `// DO NOT bump rusqlite past 0.31 — this transmute breaks at 0.34 (sqlite-vec issue #206).`
    - `pub fn init_vec_table(db: &Connection)`: `CREATE VIRTUAL TABLE IF NOT EXISTS kb_vectors USING vec0(embedding float[384])` (384 = MultilingualE5Small dim — Pitfall 3: dimension is a schema constant).
    - `pub fn insert_vector(db, chunk_rowid: i64, emb: &[f32])`: INSERT INTO kb_vectors(rowid, embedding) VALUES (?, ?) using `emb.as_bytes()` from `zerocopy::AsBytes`.
    - `pub fn knn(db, query: &[f32], k: usize) -> Result<Vec<(i64, f64)>>`: `SELECT rowid, distance FROM kb_vectors WHERE embedding MATCH ?1 ORDER BY distance LIMIT ?2` mapping `(rowid, distance)`.
    - `pub fn delete_vectors_for_rowids(db, rowids: &[i64])`: DELETE FROM kb_vectors WHERE rowid IN (...) — supports idempotent re-index (D-12).

    Declare `pub mod vector;` in lib.rs (if not already from Plan 03-00 stubs). Ensure `register_sqlite_vec()` is documented as "call ONCE at startup before opening the KB connection" (Plan 03-03 calls it from the Tauri setup).
  </action>
  <verify>
    <automated>cargo test -p nexusai-kb vector</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sqlite3_auto_extension" src-tauri/crates/nexusai-kb/src/vector.rs` matches
    - `grep "vec0(embedding float\[384\])" src-tauri/crates/nexusai-kb/src/vector.rs` matches
    - `grep "DO NOT bump rusqlite" src-tauri/crates/nexusai-kb/src/vector.rs` matches
    - `grep -rL "sqlite_vec" src-tauri/crates/nexusai-kb/src/embed.rs src-tauri/crates/nexusai-kb/src/search.rs src-tauri/crates/nexusai-kb/src/ingest.rs src-tauri/crates/nexusai-kb/src/chunk.rs` lists all four (confirms ONLY vector.rs imports sqlite_vec — D-16)
    - `cargo test -p nexusai-kb vector` passes
  </acceptance_criteria>
  <done>vector.rs is the sole sqlite-vec consumer; KNN round-trip test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embeddings (embed.rs) — fastembed E5 PT-BR</name>
  <read_first>
    - src-tauri/crates/nexusai-kb/src/embed.rs (RED #[ignore] offline test + unimplemented stubs from Plan 03-00)
    - .planning/phases/03-knowledge-base-rag/03-RESEARCH.md (§Architecture Pattern 2 — fastembed singleton verbatim; Pitfall 2 offline behavior; D-15 PT-BR multilingual requirement)
  </read_first>
  <behavior>
    - test_offline (#[ignore], GREEN when run manually network-off after cache): embed one PT-BR string, returned vector length == 384.
    - Passages are prefixed "passage: ", queries "query: " (E5 requirement).
  </behavior>
  <action>
    Implement src-tauri/crates/nexusai-kb/src/embed.rs following 03-RESEARCH.md Pattern 2:

    - `pub fn load_model(cache_dir: PathBuf) -> anyhow::Result<TextEmbedding>`: `TextEmbedding::try_new(InitOptions::new(EmbeddingModel::MultilingualE5Small).with_cache_dir(cache_dir).with_show_download_progress(true))` (D-15: MultilingualE5Small is 384-dim, strong PT-BR).
    - `pub fn embed_passages(model, texts: &[String]) -> anyhow::Result<Vec<Vec<f32>>>`: prefix each with `format!("passage: {t}")`, call `model.embed(prefixed, None)`.
    - `pub fn embed_query(model, q: &str) -> anyhow::Result<Vec<f32>>`: `model.embed(vec![format!("query: {q}")], None)?.remove(0)`.
    - Add a module-level lazy singleton for the model using `lazy_static!`/`OnceCell` keyed off the app-data cache dir, mirroring the chat crate's `CANCEL_MAP` lazy_static pattern, exposed via `pub fn global_model(cache_dir: &Path) -> anyhow::Result<&'static TextEmbedding>` OR an `Arc<TextEmbedding>` getter — choose one and document it. Plan 03-03 uses this getter so the ONNX model loads once.
    - Confirm the exact fastembed 5.17 API (`InitOptions::new`, `EmbeddingModel::MultilingualE5Small`, `.embed(Vec<String>, Option<usize>)`) against the resolved version; adjust if the builder signature differs, recording the actual API in the SUMMARY.
    - Keep the `#[ignore]` on the offline test (it needs the model pre-cached + network off — gated for CI).
  </action>
  <verify>
    <automated>cargo build -p nexusai-kb</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MultilingualE5Small" src-tauri/crates/nexusai-kb/src/embed.rs` matches
    - `grep "passage: " src-tauri/crates/nexusai-kb/src/embed.rs` and `grep "query: " src-tauri/crates/nexusai-kb/src/embed.rs` match (E5 prefixes)
    - `grep "with_cache_dir" src-tauri/crates/nexusai-kb/src/embed.rs` matches
    - a global/once-cell model getter function exists (grep for `global_model` or `OnceCell`/`lazy_static`)
    - `cargo build -p nexusai-kb` exits 0 (offline test stays #[ignore], not run in CI)
  </acceptance_criteria>
  <done>embed.rs loads MultilingualE5Small once, embeds passages/queries with E5 prefixes; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hybrid search (search.rs) — FTS5 BM25 + RRF</name>
  <read_first>
    - src-tauri/crates/nexusai-kb/src/search.rs (RED test_rrf_deterministic + unimplemented stub from Plan 03-00)
    - src-tauri/crates/nexusai-kb/src/vector.rs (knn signature it calls)
    - .planning/phases/03-knowledge-base-rag/03-RESEARCH.md (§Architecture Pattern 3 — FTS5 external-content table + triggers + bm25 sign + RRF code; Pitfall 4 score-vs-rank; Pitfall 6 unicode61 for PT-BR)
  </read_first>
  <behavior>
    - test_rrf_deterministic (GREEN): vec_hits=[10,20,30], bm25_hits=[30,40,10], k=60, top_n=3 → 10 and 30 are the top two (tie), 20/40 follow; output length == 3.
  </behavior>
  <action>
    Implement src-tauri/crates/nexusai-kb/src/search.rs following 03-RESEARCH.md Pattern 3:

    - `pub fn init_fts_table(db: &Connection) -> rusqlite::Result<()>`: create the FTS5 external-content table over kb_chunks and its sync triggers EXACTLY as Pattern 3:
      `CREATE VIRTUAL TABLE IF NOT EXISTS kb_fts USING fts5(content, content='kb_chunks', content_rowid='rowid', tokenize='unicode61')` — unicode61 for PT-BR diacritic folding (Pitfall 6: NOT porter). Add AFTER INSERT/UPDATE/DELETE triggers on kb_chunks to keep kb_fts in sync.
    - `pub fn bm25_search(db: &Connection, query: &str, k: usize) -> rusqlite::Result<Vec<i64>>`: `SELECT rowid FROM kb_fts WHERE kb_fts MATCH ?1 ORDER BY bm25(kb_fts) LIMIT ?2` returning rowids best-first (bm25() is negative → ASC = most relevant first, Pitfall 4).
    - `pub fn reciprocal_rank_fusion(vec_hits: &[i64], bm25_hits: &[i64], k: f64, top_n: usize) -> Vec<i64>`: implement EXACTLY the RRF code in Pattern 3 — `score(d) += 1/(k + rank)` (1-based rank) summed across both lists, sort descending, take top_n. Fuse over RANKS not raw scores (Pitfall 4).
    - `pub fn hybrid_search(db: &Connection, query_text: &str, query_vec: &[f32], top_n: usize) -> rusqlite::Result<Vec<i64>>`: run `bm25_search` and `vector::knn` (k = top_n*4 each for a good fusion pool), pass both rowid lists to `reciprocal_rank_fusion(.., 60.0, top_n)`, return fused rowids. This is the function Plan 03-03's `query_kb` command calls.
  </action>
  <verify>
    <automated>cargo test -p nexusai-kb rrf</automated>
  </verify>
  <acceptance_criteria>
    - `grep "reciprocal_rank_fusion" src-tauri/crates/nexusai-kb/src/search.rs` matches
    - `grep "tokenize='unicode61'" src-tauri/crates/nexusai-kb/src/search.rs` matches
    - `grep "bm25(kb_fts)" src-tauri/crates/nexusai-kb/src/search.rs` matches
    - `grep "pub fn hybrid_search" src-tauri/crates/nexusai-kb/src/search.rs` matches and references `vector::knn`
    - `cargo test -p nexusai-kb rrf` passes (deterministic fusion)
  </acceptance_criteria>
  <done>FTS5 table + BM25 + RRF fusion implemented; rrf test green; hybrid_search wired to vector::knn.</done>
</task>

</tasks>

<verification>
- `cargo test -p nexusai-kb vector` and `cargo test -p nexusai-kb rrf` pass.
- `cargo build -p nexusai-kb` compiles. Only vector.rs imports sqlite_vec (D-16).
- Offline embedding test (#[ignore]) compiles and is ready for manual network-off run (criterion #5).
</verification>

<success_criteria>
Local embeddings (E5 384-dim), sqlite-vec KNN, FTS5 BM25, and RRF fusion all implemented and tested — the retrieval engine for KB-02/KB-07 is GREEN, with the sqlite-vec alpha surface fully isolated in vector.rs (D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-base-rag/03-02-SUMMARY.md` (record the exact fastembed 5.17 API used + the model getter signature for Plan 03-03; note whether the offline test passed when run manually network-off).
</output>
