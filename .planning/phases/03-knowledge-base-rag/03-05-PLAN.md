---
phase: 03-knowledge-base-rag
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/routes/kb/-components/NoteEditor.tsx
  - src/routes/kb/index.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements: [KB-03]
must_haves:
  truths:
    - "User can create/edit a Markdown note in a CodeMirror editor that does NOT mutate the raw markdown (D-07/D-08)"
    - "The /kb route renders the hybrid two-pane browser (tree + table/editor) with the empty-state dropzone"
    - "The KB module is enabled in the sidebar and navigable"
  artifacts:
    - path: "src/routes/kb/-components/NoteEditor.tsx"
      provides: "CodeMirror 6 markdown editor, no-mutation (D-08)"
      contains: "react-codemirror|CodeMirror"
    - path: "src/routes/kb/index.tsx"
      provides: "hybrid two-pane KB browser layout (D-09)"
      contains: "ResizablePanel|FolderTree"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "KB module enabled (implemented: true)"
      contains: "kb"
  key_links:
    - from: "src/routes/kb/index.tsx"
      to: "src/routes/kb/-components/NoteEditor.tsx"
      via: "selecting a tree note opens it in the editor"
      pattern: "NoteEditor"
    - from: "src/components/layout/Sidebar.tsx"
      to: "/kb route"
      via: "MODULES kb entry implemented:true → Link active"
      pattern: "implemented: true"
---

<objective>
Complete the KB browser: build the CodeMirror notes editor (D-07/D-08, no markdown mutation), assemble the hybrid two-pane `/kb` route (tree + table, editor opens on note select, empty-state dropzone), and enable the KB module in the sidebar. Turns GREEN the `kb-notes-editor` RED test from Plan 03-00. Delivers KB-03 (create/edit/organize Markdown notes) and makes the KB module reachable.

Purpose: The last assembly step for the KB management surface — wires Plan 03-04's components into a navigable route with note editing.
Output: NoteEditor, the /kb two-pane layout, sidebar KB enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-knowledge-base-rag/03-UI-SPEC.md
@.planning/phases/03-knowledge-base-rag/03-CONTEXT.md

<interfaces>
<!-- Plan 03-04 components + existing layout patterns. -->

From Plan 03-04: `src/routes/kb/-components/ItemsTable.tsx`, `FolderTree.tsx` (exposes `onSelectNote(id)`), `ImportDropzone.tsx`, `IndexStatusBadge.tsx`; `src/lib/queries/kb.ts` (useKbItems, useKbFolders, useKbNote, useCreateNote/useReindexItem/useDeleteItem); `src/lib/stores/indexing.ts`.

From Plan 03-00 RED test — tests/kb-notes-editor.test.tsx: NoteEditor renders given markdown; onChange/onSave receives the EXACT input string unchanged (D-08 no-mutation invariant).

Existing route stub — src/routes/kb/index.tsx:
```tsx
import { createFileRoute } from "@tanstack/react-router";
import { ModuleStub } from "@/components/layout/ModuleStub";
export const Route = createFileRoute("/kb/")({
  component: () => <ModuleStub moduleName="Base de Conhecimento" />,
});
```

Existing resizable pattern — reuse `src/components/ui/resizable.tsx` (Phase 2 chat two-pane; ResizablePanelGroup/ResizablePanel/ResizableHandle). Tree pane default 280px, resizable 20%–40% (UI-SPEC layout constraints).

Existing Sidebar — src/components/layout/Sidebar.tsx MODULES array: the `kb` entry is `{ id: "kb", ..., route: "/kb", implemented: false }`. Flip to `implemented: true` (same pattern Phase 2 used to enable chat).

npm deps (installed in Plan 03-00): `@uiw/react-codemirror`, `@codemirror/lang-markdown`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NoteEditor (CodeMirror, no-mutation D-08)</name>
  <read_first>
    - tests/kb-notes-editor.test.tsx (the RED contract: no-mutation invariant)
    - src/lib/queries/kb.ts (useKbNote/useCreateNote from Plan 03-04)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Typography editor 13px mono 1.6; §Spacing notes editor p-4; §States notes editor; §Copywriting "Nova nota")
    - .planning/phases/03-knowledge-base-rag/03-RESEARCH.md (§Frontend Decisions editor — @uiw/react-codemirror + lang-markdown, compose primitives, do NOT use @uiw/react-markdown-editor)
  </read_first>
  <action>
    Create src/routes/kb/-components/NoteEditor.tsx using `@uiw/react-codemirror` + `@codemirror/lang-markdown`:
    - Props: `{ value: string; onChange: (md: string) => void; onSave?: (md: string) => void }`.
    - Render `<CodeMirror value={value} extensions={[markdown()]} onChange={onChange} />` with a 13px monospace theme at 1.6 line-height (UI-SPEC typography), `p-4` outer padding, editor background = dominant surface.
    - CRITICAL (D-08): pass the editor value straight through — do NOT trim, normalize line endings, collapse blank lines, or round-trip through any markdown serializer. The string the user types is the string emitted by onChange (the kb-notes-editor test asserts `onChange` receives the EXACT input). Raw Markdown is the stored source of truth.
    - New-note default title "Nova nota". Provide an explicit save (or debounced autosave) calling `onSave` — implementation discretion, but it MUST persist the exact text (via useCreateNote → create_note command, which writes raw .md and re-embeds per Plan 03-03). Saving re-indexes the note (status badge transitions visible via indexingStore).
    - Inline markdown syntax highlighting from `markdown()` gives the Obsidian live-preview feel WITHOUT mutating text.
  </action>
  <verify>
    <automated>npm run test -- kb-notes-editor</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "react-codemirror\|CodeMirror" src/routes/kb/-components/NoteEditor.tsx` matches
    - `grep "@codemirror/lang-markdown\|markdown()" src/routes/kb/-components/NoteEditor.tsx` matches
    - `grep "Nova nota" src/routes/kb/-components/NoteEditor.tsx` matches
    - NoteEditor does NOT call `.trim()`/`.replace(/\n/...)`/any normalizer on the value before onChange/onSave (verify by reading: value passes through unchanged)
    - `npm run test -- kb-notes-editor` passes (no-mutation invariant GREEN)
  </acceptance_criteria>
  <done>CodeMirror markdown editor renders + emits raw unchanged markdown; notes-editor test green.</done>
</task>

<task type="auto">
  <name>Task 2: /kb two-pane route assembly (D-09)</name>
  <read_first>
    - src/routes/kb/index.tsx (current ModuleStub — replace)
    - src/routes/kb/-components/* (FolderTree, ItemsTable, ImportDropzone, NoteEditor — the pieces to compose)
    - src/components/ui/resizable.tsx (ResizablePanelGroup/Panel/Handle from Phase 1/2)
    - src/lib/queries/kb.ts and src/lib/stores/indexing.ts (data + hydrateFromDb for reconcile-on-load)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Layout Structure two-pane diagram + empty state; §Spacing pane padding; §Animation row fade-in)
  </read_first>
  <action>
    Replace src/routes/kb/index.tsx ModuleStub with the hybrid two-pane browser (D-09), reusing `react-resizable-panels` (same as chat):
    - Left ResizablePanel (default 280px, min 20% / max 40%): `<FolderTree onSelectNote={setSelectedNoteId} />` ("Notas e pastas"), notes/folders only.
    - Right ResizablePanel: if a note is selected → `<NoteEditor>` bound to `useKbNote(selectedNoteId)` (opens in place of / as a tab over the table per UI-SPEC); else → `<ItemsTable>` ("Todos os itens") with faceted Tipo/Status filters.
    - Empty state: when `useKbItems()` returns zero items, render `<ImportDropzone>` (centered) instead of the table (D-13).
    - On mount, call `indexingStore.hydrateFromDb(items)` with the loaded kbItems so badges reconcile from the DB `status` column (Pitfall 5: any 'indexing' on load → 'failed'/retry).
    - Pane content padding `p-4`; reuse the Phase 2 layout shell (sidebar offset). First-run model-download state ("Baixando modelo de embeddings (uma única vez)...") may surface via the global Progress panel — wire if the indexingStore exposes it, otherwise leave a TODO comment referencing Plan 03-03's first-run progress.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ResizablePanel\|ResizablePanelGroup" src/routes/kb/index.tsx` matches
    - `grep "FolderTree" src/routes/kb/index.tsx` and "ItemsTable" and "NoteEditor" and "ImportDropzone" all match
    - `grep "hydrateFromDb" src/routes/kb/index.tsx` matches (reconcile-on-load)
    - `grep "ModuleStub" src/routes/kb/index.tsx` does NOT match (stub replaced)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>/kb renders the hybrid two-pane browser with tree, table, editor-on-select, and empty-state dropzone; reconciles status on load.</done>
</task>

<task type="auto">
  <name>Task 3: Enable KB module in Sidebar</name>
  <read_first>
    - src/components/layout/Sidebar.tsx (MODULES array — the kb entry currently implemented:false)
    - .planning/phases/02-llm-chat/02-07-SUMMARY.md (how the chat module was enabled — same pattern)
  </read_first>
  <action>
    Edit src/components/layout/Sidebar.tsx: change the `kb` entry in the MODULES array from `implemented: false` to `implemented: true` so it renders as an active `<Link to="/kb">` (not the disabled "Em breve" stub). Keep the existing icon (`BookOpen`), label ("Base de Conhecimento"), and route ("/kb"). This is the same one-line enable Phase 2 used for chat. Confirm clicking it navigates to the new /kb browser (active state `ring-2 ring-ring text-primary` applies via the existing isActive logic).
  </action>
  <verify>
    <automated>npm run test -- routes 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The `kb` MODULES entry has `implemented: true` (grep `id: "kb"` line context shows implemented:true)
    - `grep "implemented: false" src/components/layout/Sidebar.tsx` no longer matches the kb line
    - `npm run build` succeeds and `npm run test -- routes` passes (no route regressions)
  </acceptance_criteria>
  <done>KB module enabled and navigable from the sidebar.</done>
</task>

</tasks>

<verification>
- `npm run test -- kb-notes-editor` passes (D-08 no-mutation).
- `npm run build` compiles the full KB route.
- KB module enabled in sidebar; /kb renders the two-pane browser.
</verification>

<success_criteria>
Users can create/edit Markdown notes in a non-mutating CodeMirror editor, browse all KB items in the hybrid two-pane view, and reach the KB module from the sidebar (KB-03 + the assembled KB-05 browser).
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-base-rag/03-05-SUMMARY.md` (note the note save/autosave approach + how the editor/table swap is handled in the right pane).
</output>
