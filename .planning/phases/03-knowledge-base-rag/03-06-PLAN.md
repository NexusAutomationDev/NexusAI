---
phase: 03-knowledge-base-rag
plan: 06
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/routes/chat/-components/MessageInput.tsx
  - src/routes/chat/-components/MarkdownRenderer.tsx
  - src/routes/chat/-components/MessageBubble.tsx
  - src/lib/stores/chat.ts
  - src/lib/kb/citations.ts
autonomous: true
requirements: [KB-02]
must_haves:
  truths:
    - "A per-message 'Usar KB' selector beside the model picker opts a message into KB grounding (D-03)"
    - "When grounded, query_kb runs, top chunks are injected with a citation prompt, and the answer streams via the existing pipeline (D-01)"
    - "Grounded answers render inline [n] citation markers + source cards below, with a cards-only fallback (D-04/D-06)"
  artifacts:
    - path: "src/lib/kb/citations.ts"
      provides: "retriever→citation-map, citation prompt builder, per-message citation lookup"
      contains: "buildCitationPrompt"
    - path: "src/routes/chat/-components/MarkdownRenderer.tsx"
      provides: "custom [n] node → clickable accent superscript (D-04)"
      contains: "sup"
    - path: "src/routes/chat/-components/MessageBubble.tsx"
      provides: "source cards below grounded answers (D-04, always-correct → D-06 fallback)"
      contains: "Fontes"
  key_links:
    - from: "src/routes/chat/-components/MessageInput.tsx"
      to: "query_kb command"
      via: "grounded send invokes query_kb then injects chunks into stream_chat"
      pattern: "query_kb"
    - from: "src/routes/chat/-components/MarkdownRenderer.tsx"
      to: "per-message citation map"
      via: "[n] markers resolve to citation chunk metadata"
      pattern: "citation"
---

<objective>
Integrate RAG INTO the existing Phase 2 chat module (D-01, NOT a separate view): add a per-message "Usar KB" selector beside the model picker (D-03), run hybrid retrieval when grounding is on, inject top chunks + a citation prompt into the existing stream, and render inline `[n]` citation markers + source cards (D-04) with the D-06 cards-only fallback. This delivers the query/answer half of KB-02 with the success-criterion citation pattern.

Purpose: The user-facing payoff of the whole phase — asking questions answered from the KB with cited sources, reusing the shipped streaming pipeline.
Output: KB-scope selector, citation prompt/map module, citation rendering + source cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-knowledge-base-rag/03-UI-SPEC.md
@.planning/phases/03-knowledge-base-rag/03-CONTEXT.md
@.planning/phases/03-knowledge-base-rag/03-RESEARCH.md

<interfaces>
<!-- Phase 2 chat components + Plan 03-03 query_kb. -->

NOTE: The Phase 2 chat components (MessageInput.tsx, MarkdownRenderer.tsx, MessageBubble.tsx) are created by Phase 2 plans 02-05/02-06 in `src/routes/chat/-components/`. Before editing, READ the Phase 2 SUMMARYs (.planning/phases/02-llm-chat/02-*-SUMMARY.md) and the actual on-disk component files to get their current props/structure. Do NOT assume — extend the real components.

From Plan 03-03 (bindings.ts): `query_kb(input: { query: string; topK: number }) -> { chunks: Citation[] }` where `Citation = { id: string; itemId: string; itemTitle: string; kind: 'file'|'note'|'url'; section: string | null; snippet: string }`.

Existing chat store — src/lib/stores/chat.ts: `currentModel`/`setCurrentModel`, `startStream(input, onToken, onDone, onError)`, `ChatMessage { role, content, attachments? }`, `StreamChatInput { conversationId, messages, model }`.

Citation prompt scaffold (PT-BR) — 03-RESEARCH.md Pattern 5:
"Responda APENAS com base nos trechos abaixo. Cite a fonte de cada afirmação usando o número entre colchetes correspondente, ex: [1]. Se a resposta não estiver nos trechos, diga que não encontrou.\n\n[1] (arquivo: contrato.pdf, seção: Cláusula 3) <chunk text> ..."

UI-SPEC (03-UI-SPEC.md): KB-scope label off "Usar KB" / on "KB ativa", toggled state uses accent (`bg-accent text-accent-foreground`); inline `[n]` = accent `<sup>` (text-xs font-medium, link affordance, real button role="..." aria-label "Fonte 1"); source cards header "{nome} · {seção}" in `bg-secondary` under a "Fontes" group; cards-only fallback when markers weak (D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Citation map + prompt builder (citations.ts) + grounded chat flow</name>
  <read_first>
    - .planning/phases/02-llm-chat/02-*-SUMMARY.md (current chat store + stream flow + message persistence)
    - src/lib/stores/chat.ts (startStream + ChatMessage + StreamChatInput shapes)
    - src/lib/bindings.ts (query_kb signature + Citation type from Plan 03-03)
    - .planning/phases/03-knowledge-base-rag/03-RESEARCH.md (§Architecture Pattern 5 citation prompt + D-06 fallback ordering)
  </read_first>
  <action>
    Create src/lib/kb/citations.ts:
    - `export interface Citation { id, itemId, itemTitle, kind, section, snippet }` (mirror bindings).
    - `export function buildCitationPrompt(chunks: Citation[]): string`: produce the PT-BR system scaffold from Pattern 5 — the instruction line + a numbered list `[1] (arquivo: {itemTitle}, seção: {section}) {snippet}` for each chunk (label "arquivo"/"nota"/"URL" by kind). Numbering is 1-based and IS the citation ID surfaced to the LLM.
    - `export function buildCitationMap(chunks: Citation[]): Map<number, Citation>`: index→chunk, so the `[n]` renderer and source cards resolve markers to metadata. Attach this map per assistant message (store it alongside the message, e.g. a `citations?: Citation[]` field on the persisted/rendered message).
    - `export async function retrieveForQuery(query: string, topK = 6): Promise<Citation[]>`: invoke `query_kb` and return chunks (empty array on no hits → caller still sends, with prompt noting "não encontrou").

    Wire the grounded send path. In the chat store (src/lib/stores/chat.ts) add per-message KB grounding state:
    - `kbScope: boolean` + `setKbScope(on: boolean)` (the "Usar KB" toggle state, per-message).
    - A `sendGrounded` helper (or extend the existing send flow used by MessageInput): when kbScope is on, call `retrieveForQuery(userText)`, prepend a system `ChatMessage { role:'user', content: buildCitationPrompt(chunks) + userText }` OR inject the prompt as the grounding preamble into the messages array, then call the existing `startStream`. Persist the returned `chunks` with the resulting assistant message so MessageBubble can render source cards (D-04). Keep ungrounded sends unchanged — grounding is per-message (a conversation mixes both, D-03).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "buildCitationPrompt" src/lib/kb/citations.ts` and `grep "buildCitationMap" src/lib/kb/citations.ts` match
    - `grep "Responda APENAS com base nos trechos" src/lib/kb/citations.ts` matches (PT-BR prompt)
    - `grep "query_kb" src/lib/kb/citations.ts` matches (retrieveForQuery invokes it)
    - `grep "kbScope" src/lib/stores/chat.ts` matches (per-message grounding state)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Citation prompt/map + grounded retrieval flow wired into the chat store; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: KB-scope selector in MessageInput (D-03)</name>
  <read_first>
    - .planning/phases/02-llm-chat/02-06-SUMMARY.md (MessageInput structure: textarea, model picker, send/stop, drag-drop)
    - src/routes/chat/-components/MessageInput.tsx (actual on-disk component — read before editing)
    - src/lib/stores/chat.ts (kbScope/setKbScope from Task 1)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Color accent reserved item 2; §Copywriting "Usar KB"/"KB ativa"; §Component Inventory Toggle optional)
  </read_first>
  <action>
    Edit src/routes/chat/-components/MessageInput.tsx — add a KB-scope toggle beside the existing per-message model picker (D-03), styled per UI-SPEC:
    - A pill/toggle button (use shadcn Toggle or a Button with conditional classes): off label "Usar KB" (muted/ghost); on label "KB ativa" with accent (`bg-accent text-accent-foreground`, mirroring the send button's accent — UI-SPEC accent-reserved item 2). 36px control height (`h-9`) matching the model picker.
    - Bind to `kbScope`/`setKbScope` from the chat store. State is per-message: reset (or persist intentionally) consistent with how the model picker behaves — match the model-picker's per-message behavior.
    - On send: when `kbScope` is on, route through the grounded send path from Task 1 (retrieve + citation prompt + persist chunks); when off, the existing send path is unchanged.
    - Accessibility: the toggle is keyboard-focusable with `ring-2 ring-ring` focus, `aria-pressed` reflecting on/off.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Usar KB" src/routes/chat/-components/MessageInput.tsx` and `grep "KB ativa" src/routes/chat/-components/MessageInput.tsx` match
    - `grep "kbScope" src/routes/chat/-components/MessageInput.tsx` matches
    - `grep "bg-accent" src/routes/chat/-components/MessageInput.tsx` matches (on-state accent)
    - `grep "aria-pressed" src/routes/chat/-components/MessageInput.tsx` matches (accessibility)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>"Usar KB" per-message selector beside the model picker, accent on-state, routes grounded sends.</done>
</task>

<task type="auto">
  <name>Task 3: Citation rendering — [n] markers + source cards (D-04/D-06)</name>
  <read_first>
    - .planning/phases/02-llm-chat/02-05-SUMMARY.md (MessageBubble + MarkdownRenderer structure, react-markdown ^10 usage)
    - src/routes/chat/-components/MarkdownRenderer.tsx and MessageBubble.tsx (actual on-disk — read before editing)
    - src/lib/kb/citations.ts (buildCitationMap + Citation type from Task 1)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Layout "Citations in chat"; §Color accent item 1; §Copywriting source-card headers; §States citation states; §Accessibility citation buttons)
  </read_first>
  <action>
    Edit src/routes/chat/-components/MarkdownRenderer.tsx — add a custom react-markdown node (or small rehype/text-splitting component) that detects `[\d+]` in the answer text and wraps each in a clickable accent `<sup>` (UI-SPEC: `text-xs font-medium`, accent color, link affordance). Each marker is a real button: `role`/`aria-label` "Fonte {n}", keyboard-activatable (not a click-only span). On activate, scroll to / highlight the matching source card. Resolve `n` against the per-message citation map passed in as a prop. If no citation map is present (ungrounded message), render `[n]` as plain text (no wrapping).

    Edit src/routes/chat/-components/MessageBubble.tsx — for grounded assistant messages (those carrying `citations: Citation[]`), render a "Fontes" group of source cards BELOW the answer body:
    - Cards in `bg-secondary` surface, `border-border`, `p-3`, `gap-2`, `mt-2`. Header per UI-SPEC: file → "{itemTitle} · {section}" (e.g. "contrato.pdf · Cláusula 3"); note → "{itemTitle} · {section}". Filename weight 500, section/page meta `text-muted-foreground` weight 400.
    - Pass the citation map into MarkdownRenderer so inline markers resolve.
    - D-06 fallback: the cards are driven by the retriever's `citations` array (always correct, independent of the model's text). If inline `[n]` markers are unreliable/absent, the message still shows the source cards — implement so cards render whenever `citations` is non-empty, regardless of whether markers were found in the text. (No extra copy for the fallback per UI-SPEC.)
    - Ungrounded messages render exactly as before — no "Fontes", no cards.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "sup" src/routes/chat/-components/MarkdownRenderer.tsx` matches (superscript marker)
    - `grep "Fonte" src/routes/chat/-components/MarkdownRenderer.tsx` matches (aria-label "Fonte {n}")
    - `grep "Fontes" src/routes/chat/-components/MessageBubble.tsx` matches (source-cards group)
    - `grep "bg-secondary" src/routes/chat/-components/MessageBubble.tsx` matches (card surface)
    - `grep "citations" src/routes/chat/-components/MessageBubble.tsx` matches (cards render off the retriever array → D-06 fallback)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Inline accent [n] citation buttons + source cards render for grounded answers; cards-only D-06 fallback intact.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles all chat-side edits.
- Manual (checkpoint plan 03-07): grounded query returns a cited answer; source cards point to correct chunks.
- Ungrounded chat unchanged (no regression to Phase 2 behavior).
</verification>

<success_criteria>
RAG is integrated into the existing chat surface (D-01): a per-message "Usar KB" toggle grounds the message, hybrid retrieval feeds a citation prompt into the shipped stream, and answers render inline [n] markers + source cards with a model-independent cards-only fallback (KB-02 success-criterion citation pattern).
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-base-rag/03-06-SUMMARY.md` (note how citations are persisted with messages + the grounded-send injection approach).
</output>
