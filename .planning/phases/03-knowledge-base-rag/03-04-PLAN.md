---
phase: 03-knowledge-base-rag
plan: 04
type: execute
wave: 4
depends_on: ["03-00", "03-03"]
files_modified:
  - src/lib/stores/indexing.ts
  - src/lib/queries/kb.ts
  - src/components/ui/progress.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/table.tsx
  - src/routes/kb/-components/ItemsTable.tsx
  - src/routes/kb/-components/IndexStatusBadge.tsx
  - src/routes/kb/-components/ImportDropzone.tsx
  - src/routes/kb/-components/FolderTree.tsx
autonomous: true
requirements: [KB-05]
must_haves:
  truths:
    - "A single Zustand indexingStore drives badge/progress/toast from Channel IndexProgress events, reconciled with the DB status column on reload"
    - "The KB browser shows all items in a filterable table (type + status facets) and notes/folders in a tree"
    - "An empty KB shows the PT-BR drop-zone empty state with file picker + URL paste"
  artifacts:
    - path: "src/lib/stores/indexing.ts"
      provides: "indexingStore (D-11) — single source for badge/progress/toast"
      contains: "useIndexingStore"
    - path: "src/routes/kb/-components/ItemsTable.tsx"
      provides: "TanStack Table flat view + faceted filters (D-09/D-10)"
      contains: "Nenhum item encontrado para esse filtro"
    - path: "src/routes/kb/-components/IndexStatusBadge.tsx"
      provides: "status badge selector off indexingStore (D-11/D-12)"
      contains: "indexingStore|useIndexingStore"
  key_links:
    - from: "src/lib/stores/indexing.ts"
      to: "import_file/add_url Channel"
      via: "Channel<IndexProgress> onmessage applies events"
      pattern: "IndexProgress|onmessage"
    - from: "src/routes/kb/-components/ItemsTable.tsx"
      to: "src/lib/queries/kb.ts"
      via: "useKbItems query feeds table rows"
      pattern: "useKbItems"
---

<objective>
Build the KB browser data + presentation layer: the single `indexingStore` (D-11), TanStack Query CRUD for KB items/notes/folders, and the two-pane browser components (tree + filterable table + empty-state dropzone + status badges). Turns GREEN the `kb-indexing-store` and `kb-items-table` RED tests from Plan 03-00. This delivers KB-05 (file-explorer view with indexed status).

Purpose: The management surface users browse and import into; the indexingStore is the reconciliation point between transient Channel progress and the durable DB `status` column.
Output: indexing store, kb queries, new shadcn primitives, and KB browser components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-knowledge-base-rag/03-UI-SPEC.md
@.planning/phases/03-knowledge-base-rag/03-CONTEXT.md

<interfaces>
<!-- Contracts from Plan 03-00 tests + Plan 03-03 bindings + existing patterns. -->

From Plan 03-00 RED tests this plan must satisfy:
- `useIndexingStore` exported from src/lib/stores/indexing.ts; initial empty `items`; applying started→'indexing', indexed→'indexed', failed→'failed'+reason.
- ItemsTable in src/routes/kb/-components/ItemsTable.tsx renders items, status facet 'failed' narrows rows, empty filter shows "Nenhum item encontrado para esse filtro."

From Plan 03-00 schema — src/lib/db/schema.ts: `kbItems` (id, kind, title, sourcePath, folderId, status, errorReason, createdAt, updatedAt, deletedAt), `kbFolders`, `kbChunks`; types KbItem/KbFolder.

Existing Zustand pattern with Channel — src/lib/stores/chat.ts:
```typescript
import { create } from 'zustand';
import { invoke, Channel } from '@tauri-apps/api/core';
// startStream: new Channel<StreamEvent>(); channel.onmessage = e => set(...); await invoke('stream_chat', { input, onEvent: channel });
```
IndexProgress shape (from Plan 03-03 bindings, tagged): `{ event: 'started'|'chunk'|'indexed'|'failed', data: {...} }`.

Existing TanStack Query pattern — src/lib/queries/chat.ts: query-key factory, `db` proxy from '@/lib/db/proxy', `eq/isNull/desc` from drizzle-orm, ALWAYS filter `isNull(deletedAt)` (soft-delete).

UI-SPEC copy + color (03-UI-SPEC.md): status labels Pendente/Indexando/Indexado/Falhou; badge variants secondary (pending/indexing/indexed muted) / destructive (failed); empty-state copy "Sua base de conhecimento está vazia" etc.; facet labels "Tipo"/"Status".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: indexingStore + KB queries</name>
  <read_first>
    - tests/kb-indexing-store.test.ts (the RED contract this satisfies)
    - src/lib/stores/chat.ts (Zustand + Channel pattern to mirror)
    - src/lib/queries/chat.ts (TanStack Query pattern: query keys, db proxy, soft-delete filter)
    - src/lib/db/schema.ts (kbItems/kbFolders types)
    - src/lib/bindings.ts (KB command signatures from Plan 03-03)
  </read_first>
  <action>
    Create src/lib/stores/indexing.ts (D-11) — the single source of truth for indexing UI:
    - `IndexProgress` TS type mirroring the Rust tagged enum: `{ event: 'started'; data: { itemId: string; totalChunks: number | null } } | { event: 'chunk'; data: { itemId: string; done: number; total: number | null } } | { event: 'indexed'; data: { itemId: string } } | { event: 'failed'; data: { itemId: string; reason: string } }`.
    - State: `items: Record<string, { status: 'pending'|'indexing'|'indexed'|'failed'; reason?: string; done?: number; total?: number | null }>`.
    - `useIndexingStore` (Zustand `create`) with:
      - `apply(ev: IndexProgress)`: started→set status 'indexing' (+total); chunk→update done/total; indexed→'indexed'; failed→'failed' + reason.
      - `setPending(itemId)`: seed a row as 'pending' (used the moment an item is added before the Channel fires).
      - `hydrateFromDb(items: { id: string; status: ...; errorReason?: string }[])`: reconcile on reload — copy DB status into the store; any item whose DB status is 'indexing' at hydrate time is forced to 'failed' (Pitfall 5: dropped-event stuck reconcile).
      - `startIndexing(commandName, input)`: helper that creates `new Channel<IndexProgress>()`, wires `onmessage = apply`, and `await invoke(commandName, { input, onEvent: channel })` — mirrors chat.ts startStream.

    Create src/lib/queries/kb.ts (TanStack Query, mirror queries/chat.ts):
    - query-key factory `kbKeys` (items, item(id), folders, notesInFolder(id)).
    - `useKbItems()`: select from kbItems where `isNull(deletedAt)`, order by updatedAt desc.
    - `useKbFolders()`: select from kbFolders.
    - `useKbNote(id)`: select a single note item + read its markdown content (via a `read_note_content` path or store source_path — if not exposed, read the kb_chunks/content; simplest: add the note content to kbItems query or invoke a small command — coordinate with Plan 03-05 which owns the editor; for now expose the item row).
    - Mutations: `useAddUrl()` (invoke add_url via indexingStore.startIndexing), `useImportFiles()`, `useReindexItem()` (invoke reindex_item), `useDeleteItem()` (invoke delete_item) — each invalidates kbKeys.items on success.
  </action>
  <verify>
    <automated>npm run test -- kb-indexing-store</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useIndexingStore" src/lib/stores/indexing.ts` matches
    - `grep "hydrateFromDb" src/lib/stores/indexing.ts` matches and forces 'indexing'→'failed' on reload (grep for the reconcile logic)
    - `grep "useKbItems" src/lib/queries/kb.ts` and `grep "isNull" src/lib/queries/kb.ts` match (soft-delete filter)
    - `grep "useReindexItem\|reindex_item" src/lib/queries/kb.ts` matches
    - `npm run test -- kb-indexing-store` passes (store transitions GREEN)
  </acceptance_criteria>
  <done>indexingStore + kb queries implemented; indexing-store test green.</done>
</task>

<task type="auto">
  <name>Task 2: New shadcn primitives + status badge</name>
  <read_first>
    - src/components/ui/badge.tsx (existing variants — secondary/destructive used by the status badge)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Color status-badge semantics table; §Component Inventory "New shadcn components"; §Copywriting status labels)
    - src/lib/stores/indexing.ts (Task 1 — the store the badge selects off)
  </read_first>
  <action>
    Add the three new shadcn components via manual copy from the official shadcn v4 Radix registry (NO `npx shadcn add` — maintain the Phase 1/2 manual-copy pattern), placed in src/components/ui/:
    - progress.tsx (shadcn Progress) — global batch progress panel.
    - sonner.tsx (shadcn Sonner/Toaster) — backed by the `sonner` npm package installed in Plan 03-00. Also mount `<Toaster />` in the app root (AppShell/root layout) if not already mounted.
    - table.tsx (shadcn Table) — base for the data-table.

    Create src/routes/kb/-components/IndexStatusBadge.tsx — a pure selector off `useIndexingStore`, reconciled with the row's DB `status`. Implement EXACTLY the 03-UI-SPEC color/label table:
    - pending → `<Badge variant="secondary">` "Pendente" (muted).
    - indexing → `<Badge variant="secondary">` "Indexando" + `<Loader2 className="animate-spin" size={14} />`.
    - indexed → `<Badge variant="outline">`/secondary muted "Indexado" + `<Check size={14} />`.
    - failed → `<Badge variant="destructive">` "Falhou" + `<AlertCircle size={14} />`, wrapped in a `<Tooltip>` showing `error_reason`, plus a "Reindexar" action (button/row-menu) calling useReindexItem.
    - Each badge carries `aria-label` of the status (accessibility: color is not the only signal — text + glyph).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: src/components/ui/progress.tsx, src/components/ui/sonner.tsx, src/components/ui/table.tsx
    - `grep "Pendente" src/routes/kb/-components/IndexStatusBadge.tsx` and "Indexando" and "Indexado" and "Falhou" all match
    - `grep "variant=\"destructive\"" src/routes/kb/-components/IndexStatusBadge.tsx` matches (failed)
    - `grep "Reindexar" src/routes/kb/-components/IndexStatusBadge.tsx` matches (D-12 retry)
    - `grep "animate-spin" src/routes/kb/-components/IndexStatusBadge.tsx` matches (indexing spinner)
    - `npm run build` succeeds (TypeScript compiles)
  </acceptance_criteria>
  <done>Progress/Sonner/Table primitives added; status badge renders all four states with PT-BR labels + glyphs.</done>
</task>

<task type="auto">
  <name>Task 3: ItemsTable + FolderTree + ImportDropzone</name>
  <read_first>
    - tests/kb-items-table.test.tsx (the RED contract this satisfies: render, status facet 'failed', empty-filter copy)
    - src/routes/kb/-components/IndexStatusBadge.tsx (Task 2 — badge cell)
    - src/lib/queries/kb.ts (useKbItems/useKbFolders/useImportFiles/useAddUrl from Task 1)
    - .planning/phases/03-knowledge-base-rag/03-UI-SPEC.md (§Layout Structure two-pane; §Interaction Patterns ingestion/URL/filter; §Copywriting empty-state + facet copy)
    - src/lib/bindings.ts (pick_and_encode_file exists from chat; for KB use a file dialog → import_file)
  </read_first>
  <action>
    Create src/routes/kb/-components/ItemsTable.tsx (TanStack Table v8 + shadcn Table, D-09/D-10):
    - Columns: type icon (lucide `FileText`/`StickyNote`/`Link` by kind), title, indexed-status badge (`<IndexStatusBadge>`), row actions (dropdown-menu: "Reindexar", "Excluir" with Alert-Dialog confirm "Excluir item da base? Esta ação não pode ser desfeita.").
    - Faceted filters toolbar (gap-2) on `Tipo` (Arquivo/Nota/URL) and `Status` (Pendente/Indexando/Indexado/Falhou) using TanStack column filters.
    - Empty filtered result row renders exactly "Nenhum item encontrado para esse filtro."
    - Rows fed from `useKbItems()`; status reflects indexingStore reconciled with row.status.

    Create src/routes/kb/-components/FolderTree.tsx (react-arborist, D-09 left pane, notes/folders ONLY per D-10):
    - Heading "Notas e pastas". Dense 28px rows. Build the tree from `useKbFolders()` + note items (kind==='note'). Selecting a note calls an `onSelectNote(id)` prop (consumed by Plan 03-05's editor wiring); selecting a folder optionally filters the table. Files/URLs are NEVER placed in the tree.

    Create src/routes/kb/-components/ImportDropzone.tsx (D-13 empty state):
    - Dashed `border-2 border-dashed`, drag-over → `border-accent bg-accent/5`. Headline "Sua base de conhecimento está vazia"; body "Arraste PDF, Markdown, DOCX ou TXT aqui — ou escolha arquivos e cole uma URL para indexar."; drop-zone affordance "Arraste PDF, Markdown, DOCX ou TXT aqui"; "Escolher arquivos" button (opens Tauri file dialog → useImportFiles → import_file per file, seeding indexingStore.setPending); URL input placeholder "Cole uma URL para indexar..." + "Adicionar" button (useAddUrl → toast "URL adicionada. Indexando..."). No demo/sample seeding.
    - On drop, accept dropped file paths and call import_file via the same flow (reuse the chat drag-drop file-path encoding approach if needed).
  </action>
  <verify>
    <automated>npm run test -- kb-items-table</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Nenhum item encontrado para esse filtro" src/routes/kb/-components/ItemsTable.tsx` matches
    - `grep "Tipo" src/routes/kb/-components/ItemsTable.tsx` and "Status" match (faceted filters)
    - `grep "IndexStatusBadge" src/routes/kb/-components/ItemsTable.tsx` matches
    - `grep "Sua base de conhecimento está vazia" src/routes/kb/-components/ImportDropzone.tsx` matches
    - `grep "Cole uma URL para indexar" src/routes/kb/-components/ImportDropzone.tsx` and "Adicionar" and "Escolher arquivos" match
    - `grep "Notas e pastas" src/routes/kb/-components/FolderTree.tsx` matches
    - `npm run test -- kb-items-table` passes
  </acceptance_criteria>
  <done>Table (faceted filters + actions), tree (notes/folders only), and dropzone empty-state built; items-table test green.</done>
</task>

</tasks>

<verification>
- `npm run test -- kb-indexing-store kb-items-table` pass.
- `npm run build` compiles (new shadcn primitives + browser components type-check).
</verification>

<success_criteria>
The KB browser data + UI exists: a single indexingStore reconciled with the DB status column drives all status surfaces, items appear in a faceted-filterable table with type/status, notes/folders in a tree, and an empty KB shows the PT-BR drop-zone import affordances (KB-05).
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-base-rag/03-04-SUMMARY.md` (record the indexingStore API + the import/url invoke flow so Plan 03-05/03-06 reuse it consistently).
</output>
