---
phase: 03-knowledge-base-rag
plan: 07
type: execute
wave: 6
depends_on: ["03-05", "03-06"]
files_modified: []
autonomous: false
requirements: [KB-01, KB-02, KB-03, KB-04, KB-05, KB-06, KB-07]
must_haves:
  truths:
    - "A real file is imported, indexed, and a grounded chat question is answered with a correct source citation"
    - "A URL is scraped, indexed, and becomes queryable; a note is created/edited and retrievable"
    - "Embeddings work offline against already-indexed content (criterion #5)"
  artifacts: []
  key_links: []
---

<objective>
Human smoke-test checkpoint validating the manual-only behaviors that automated tests cannot assert: end-to-end RAG answer quality + citation correctness, URL "queryable within one minute", note editing fidelity, and offline embedding against indexed content. Per 03-VALIDATION.md §Manual-Only Verifications. This is the phase gate before `/gsd:verify-work`.

Purpose: Confirm the full Phase 3 loop works for a real user with real PT-BR content + a live cloud LLM.
Output: User sign-off (or a diagnosed gap list for `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-knowledge-base-rag/03-VALIDATION.md
@.planning/phases/03-knowledge-base-rag/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated sweep</name>
  <files>none (read-only verification run)</files>
  <read_first>
    - .planning/phases/03-knowledge-base-rag/03-VALIDATION.md (full-suite command + offline test)
  </read_first>
  <action>
    Before pausing for the human, run the full automated suite and the build to confirm nothing regressed across the phase:
    - `npm run test` (frontend Vitest, incl. kb-indexing-store / kb-items-table / kb-notes-editor).
    - `cargo test --workspace` (Rust, incl. nexusai-kb chunk/vector/rrf/scrape; the offline test stays `#[ignore]`).
    - `npm run build` (frontend type-check + bundle).
    Report any red test or build failure. If anything fails, STOP and surface it — do not proceed to the human checkpoint with a red suite.
  </action>
  <verify>
    <automated>npm run test && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 (all frontend KB tests green)
    - `cargo test --workspace` exits 0 (all Rust KB tests green; offline test ignored)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Full automated suite + build green before the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — full KB + RAG loop</name>
  <files>none (manual verification)</files>
  <action>Pause for the user to manually verify the manual-only KB behaviors below; resume on their signal.</action>
  <what-built>
    The complete Phase 3 Knowledge Base + RAG loop: file/URL/note ingestion with local fastembed embeddings + sqlite-vec/FTS5 hybrid retrieval, a two-pane KB browser with indexed-status badges, a CodeMirror notes editor, and RAG-grounded chat with inline [n] citations + source cards.
  </what-built>
  <how-to-verify>
    Run the app (`npm run tauri dev`). Perform each manual check from 03-VALIDATION.md §Manual-Only Verifications, in PT-BR content where possible:

    1. KB-01 + KB-02 (RAG + citation):
       - Go to /kb, drop or pick a known PDF/.md/.docx (PT-BR content). Watch the badge go pending → indexing → indexed.
       - Go to chat, toggle "Usar KB" (it should show "KB ativa" with accent), ask a question whose answer is in that document.
       - Confirm: the answer is grounded in the doc, an inline [1] marker appears, and the "Fontes" source card below points to the correct file + section. (If markers are weak, confirm the cards-only D-06 fallback still shows correct sources.)

    2. KB-04 (URL within one minute):
       - In /kb, paste a real article URL, click "Adicionar". Confirm toast "URL adicionada. Indexando..." then "URL indexada e pronta para consulta." Ask a grounded question about the article — it answers. (A JS-heavy SPA URL should fail gracefully with "Conteúdo não encontrado (página dinâmica).")

    3. KB-03 (notes):
       - Create "Nova nota", type Markdown with deliberate spacing/headings, save. Reopen — confirm the text is byte-for-byte what you typed (no normalization). Ask a grounded question whose answer is in the note — it's retrieved.

    4. KB-05 (browser): confirm files/notes/URLs all appear in the table with correct type icons + status badges; faceted Tipo/Status filters narrow the list; notes/folders appear in the left tree (files/URLs do NOT).

    5. KB-07 + criterion #5 (offline): with content already indexed, disable network and re-run a grounded query against the indexed content — embedding the query + retrieval must still work offline (the cloud LLM answer step will fail without network, but retrieval/embedding must succeed; verify via the local model not erroring). Optionally run `cargo test -p nexusai-kb offline -- --ignored` with network off.

    6. KB-06 (shared index): confirm there is a single KB store (no per-agent scoping visible) — all imported items are queryable from the one chat surface.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list the specific failures (which KB-XX, what happened) so they can be routed to `/gsd:plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<verification>
- Full automated suite + build green (Task 1).
- Human confirms all six manual KB behaviors (Task 2).
</verification>

<success_criteria>
The user has verified end-to-end that they can import files/URLs/notes, get RAG-grounded answers with correct citations, browse items with status, and query indexed content offline — Phase 3 success criteria #1–#5 are met.
</success_criteria>

<output>
After approval, create `.planning/phases/03-knowledge-base-rag/03-07-SUMMARY.md` recording the checkpoint outcome (approved / gaps found). If gaps, list them for `/gsd:plan-phase --gaps`.
</output>
